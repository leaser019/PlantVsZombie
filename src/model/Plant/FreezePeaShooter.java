package model.Plant;
import static model.HelperMethod.Constant.Plants.freezePeaShooter;
public class FreezePeaShooter extends Plant {

    public FreezePeaShooter(int x, int y, int id) {
        super(x, y, id, freezePeaShooter);
    }
    
}
